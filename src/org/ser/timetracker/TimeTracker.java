package org.ser.timetracker;

import android.app.Activity;
import android.os.Bundle;
import android.widget.Button;

public class TimeTracker {
    public static final String AUTHORITY = "net.ser1.provider.TimeTracker";
    
    /** Called when the activity is first created.
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.main);
        Button start_button = (Button)findViewById(R.id.start_button);
        //Intent projectList = new Intent(Intent.ACTION_VIEW, Uri.parse("content://org.ser1.net.ProjectList"));
        //startActivity(projectList);
    }
    */
}